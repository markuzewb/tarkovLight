# ОПЦИОНАЛЬНО. Приложение работает и без этого списка: математика и захват
# экрана написаны на чистом Python (см. app/correction.py, app/capture.py).
# Ставьте, если хотите быстрее считать и иметь запасные бэкенды захвата:
#   numpy  -> анализ кадра ~0.9 мс вместо ~4 мс на кадр (12 Гц => ~0.4% vs ~5% ядра)
#   mss    -> второй бэкенд захвата, если GDI-путь на вашем драйвере не устроит
#   Pillow -> третий бэкенд захвата + tools/preview.py без него не работает
numpy>=1.24
mss>=9.0
Pillow>=9.0
